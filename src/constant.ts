export enum respCode {
  success = 200,
  cancel = 0,
}

export const enableOptions = [
  {
    label: '启用',
    value: 1,
  },
  {
    label: '停用',
    value: 0,
  },
];

export enum enableState {
  enable = 1,
  disabled = 0,
}
